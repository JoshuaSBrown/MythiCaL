#include <iostream>

#include "cluster.h"

using namespace std;
/*
#ifdef _E_
#define Err 1
#else
#define Err 0
#endif
*/
int main(void){
	/*
	if(Err==1) cout<<"Error Reporting On"<<endl;
	//Testing the Threshold Setter
	{
		assert(setThresh(10));
		assert(getThresh()==10);
		assert(setThresh(-20)==0);
	}
	//Testing the Constructor
	{
		shared_ptr<cluster> tmp = cluster();
		vector<double> nRates = {1.0,2.0};
		vector<double> nId = {2,3};
		int vFreq = 1;
		int sId = 1;
		shared_ptr<site> site1 = site(sId,nRates,nId,vFreq);
		tmp.addSite(site1);
	}
	test.printClusterInfo();
	return 0;
	*/
}
